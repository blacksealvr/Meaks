document.addEventListener('DOMContentLoaded',()=>{const el=document.getElementById('games-grid');if(!el)return;el.innerHTML=GAMES.map(g=>`<article class="card game"><span class="tag">${g.status}</span><h3>${g.name}</h3><p>${g.description}</p><div class="actions">${g.website?`<a class="small" href="${g.website}" target="_blank">Game Site</a>`:''}${g.download?`<a class="small" href="${g.download}" target="_blank">Get Game</a>`:''}${g.discord?`<a class="small" href="${g.discord}" target="_blank">Discord</a>`:''}${g.youtube?`<a class="small" href="${g.youtube}" target="_blank">YouTube</a>`:''}</div></article>`).join('')});
